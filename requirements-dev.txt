# Off-device development deps for the Windows PySide6 + QML mock (qt_app.py).
# These are NOT the Pi runtime deps (kivy, smbus2, mido, ...) — those live in
# the box's gcamp6s-venv (see REFERENCES.md). This file is just enough to run
# the mock with fake backend + fake hardware on a dev machine.
#
# Setup (Windows; `py` = the real launcher, see notes):
#   py -m venv .venv
#   .venv\Scripts\python -m pip install -r requirements-dev.txt
# Run the mock (once qt_app.py exists, Step D):
#   .venv\Scripts\python qt_app.py

PySide6==6.11.1   # Qt Quick (QML) + PySide6 GUI toolkit
requests==2.34.2  # core dep imported at modepctrl top (unused at runtime in fake mode)
